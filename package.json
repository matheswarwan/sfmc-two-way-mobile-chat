{
  "name": "sfmc-two-way-mobile-chat",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Two-way conversation app for Salesforce Marketing Cloud messaging channels",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "PORT=${API_PORT:-3000} npm run dev --workspace=server & API_PORT=${API_PORT:-3000} npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server",
    "lint": "eslint .",
    "check:sfmc": "npm run check --workspace=server"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
